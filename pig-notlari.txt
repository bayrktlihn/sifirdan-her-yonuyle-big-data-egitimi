type_names = [int, long, float, double, chararray, byarray, boolean, Tuple, Bag, Map, Null]
arithmetic_operators = [+, -, *, /, %, ?:]
comparison_operators = [==, !=, <, >, <=, >=, matches]
logical_operators = [or, and, not]
null_operators = [is null, is not null]
functions = [DUMP, DISTINCT, LOAD, FILTER]

DATA = LOAD hdfs_path USING PigStorage(separation_symbol) AS
(
	col_name_one : type_name,
	col_name_two : type_another_name
	.
	.
	. 
)

FILTERED_DATA = FILTER DATA BY col_name_one comparison_logical_operator col_name_any_value (and or) ;

DUMP DATA

pig pig_file_path
